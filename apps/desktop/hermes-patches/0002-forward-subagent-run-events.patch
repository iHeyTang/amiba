diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -6282,11 +6282,20 @@ class APIServerAdapter(BasePlatformAdapter):
                     "ref_count": kwargs.get("moa_ref_count"),
                 })
-            elif event_type in {"subagent.start", "subagent.complete"}:
+            elif event_type in {
+                "subagent.spawn_requested",
+                "subagent.start",
+                "subagent.tool",
+                "subagent.progress",
+                "subagent.thinking",
+                "subagent.complete",
+            }:
                 event = {
                     "event": event_type,
                     "run_id": run_id,
                     "timestamp": ts,
                 }
+                if tool_name:
+                    event["tool_name"] = str(tool_name)
                 if preview is not None:
                     event["preview"] = redact_sensitive_text(
                         str(preview), force=True
@@ -6326,9 +6336,5 @@ class APIServerAdapter(BasePlatformAdapter):
                         value = redact_sensitive_text(value, force=True)
                     event[key] = value
                 _push(event)
-            # _thinking, subagent.tool, and subagent_progress are intentionally
-            # not forwarded on the /v1/runs stream: they are high-volume UI
-            # noise. Lifecycle boundaries (start/complete) still need to land
-            # so clients can observe delegate_task timeouts and failures.
 
         return _callback
