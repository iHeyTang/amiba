/** Shared product default for desktop, messaging, and scheduled sessions. */
export {
  getDefaultWorkspaceRoot,
  ensureDefaultWorkspaceRoot,
} from "@amiba/dsh-plugin-session-features/default-workspace";
