/**
 * Entry point for the webview bridge preload bundle.
 * Bundled to `out/preload/webview-bridge.js` by electron-vite.
 * Injected into every extension WebView (hermes-ext://<id>/<view>).
 */
export * from "@hermes-x/extension-host/webview-preload"
