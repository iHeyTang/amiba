# 正式包启动资源优化（2026-09-21）

## 结论

在保留全部 38 条工作区绑定、启动阶段继续预热 Quick-Ask 的情况下，两轮 macOS 正式包对比中，Electron 主进程峰值物理内存从约 2,660 MiB 降至 89 MiB（约 -97%），启动后 65 秒内累计 CPU 时间从约 112 秒降至 1.2–1.8 秒（约 -99%）。

整个进程树的同期峰值内存从 4,270–4,690 MiB 降至 1,867–2,205 MiB；累计 CPU 时间从 141 秒左右降至 24–33 秒。两轮平均分别减少约 55% 和 80%。剩余最大开销是 DSH 运行时；本次未修改任何 `@deepseek-ai/*` 依赖源码、版本、锁文件或补丁。

## 做了什么

1. **绑定工作区只保存元数据，不再为每个历史会话递归监听整个项目。** 原本 38 个会话恢复时会建立 38 个目录监听，其中 29 个指向同一个大目录，重复遍历、路径对象和事件处理堆积在 Electron 主进程。工作目录、会话绑定及持久化语义保留。
2. **旧文件预览和本地资源观察 API 都按打开的文件订阅。** 相同规范化根和文件共享一个原生监听；每次订阅先做路径授权。最后一个订阅关闭即释放，窗口导航、销毁、工作区重绑定和初始化中取消都能清理，仍能感知文件删除、重新创建及文件被目录替换。新 DSH 资源提供器自己的通知机制保持原样。
3. **空闲启动时，桌宠活动摘要不再冷读上一段完整会话日志。** 使用索引里的标题和 idle 状态；运行中的会话继续跟踪日志，启动后新发生的短任务即使在两次轮询之间结束，也会获取终态。已订阅任务保留到终态事件，拒绝旧会话迟到事件。本项只改变展示摘要，不改变执行、审批或历史恢复。
4. **自有 UI shell 内嵌的 PDF 二进制资源逐项无损压缩。** 字体、CMap、WASM 仍全部随包提供，只在 PDF.js 请求对应资源时解压；不留下永久解压缓存。189 项资源逐字节一致，许可证保留。资源 base64 合计从 4,611,428 降至 2,748,312 字节；自有 shell 的实际 `client.js` 从 13,037,415 降至 11,174,456 字节（-14.3%）。这不意味着 DSH 整体内存同比下降。

Quick-Ask 窗口创建和预热时机未改。随任务另行修正了基线已有的两个 worker 类型导入及桌面 TypeScript 检查配置，使完整类型检查可运行。

## 测量方式

- 基线：`origin/main` 的 `d9b67021`；优化版：本 PR。两者均为完整 production build 后按发布排除规则暂存 runtime、通过 electron-builder 生成的 macOS arm64 `.app`，Electron 33.4.11，应用版本 0.1.9。
- 本地 ad-hoc 签名并通过 `codesign --verify --deep --strict`。未发布安装包、未进行开发者签名/公证，未覆盖 `/Applications/Amiba.app`。
- Apple M4 Pro、14 逻辑核、48 GiB 内存、macOS 26.6.2。
- 每轮使用同一份实际用户数据快照的独立 APFS 克隆，保留 38 条绑定、4 个实际目录以及原有插件配置；直接启动包内可执行文件。未删除绑定来制造改善。
- 顺序为基线 1、优化 1、基线 2、优化 2；每轮 65 秒，无用户交互，不并行构建/测试或运行另一份 Amiba。没有开发服务器、Node inspector 或 renderer CDP；功能 QA 的调试连接与解除后台绘制限速参数不用于性能测量。
- 每约 250 ms 通过 macOS `proc_pid_rusage` 采集进程树，CPU user+system 计数按 Mach timebase 转换为秒；100% CPU 表示一个逻辑核。主进程峰值用内核 lifetime physical-footprint 最大值；进程树峰值是同一采样时刻的各存活进程 physical footprint 之和，**不是各进程独立峰值相加**。
- 各轮均在启动阶段对 main/DSH 运行相同的 `sample`，末段采集 `vmmap`。数据为本机两个独立进程启动样本，不是跨平台结论，也不是清空操作系统磁盘缓存的冷机测试。

## 原始结果摘要

| 指标 | 基线 1 | 优化 1 | 基线 2 | 优化 2 |
| --- | ---: | ---: | ---: | ---: |
| 主进程峰值内存（MiB） | 2,661 | 89 | 2,660 | 89 |
| 主进程累计 CPU（秒） | 111.51 | 1.22 | 112.17 | 1.80 |
| 主进程瞬时 CPU 峰值（%） | 442 | 87 | 454 | 65 |
| 进程树同时峰值内存（MiB） | 4,270 | 1,867 | 4,690 | 2,205 |
| 进程树累计 CPU（秒） | 141.52 | 23.72 | 140.66 | 32.77 |
| 65 秒末进程树内存（MiB） | 4,259 | 1,535 | 3,100 | 1,724 |
| DSH 峰值内存（MiB） | 1,350 | 1,136 | 1,271 | 1,174 |
| DSH 累计 CPU（秒） | 20.12 | 17.08 | 19.94 | 19.56 |
| Quick-Ask renderer 峰值内存（MiB） | 62 | 62 | 63 | 63 |

进程树包含 GPU、renderer、utility 及插件/MCP 子进程。GPU、渲染和 GC 时机有波动，末段数值尤其不能等同于长期稳定占用。主进程的巨大改善在两次测量中一致；DSH 的小幅差异不足以单独归因于某一项优化。

## 验证

- 桌面测试：144/144 通过，覆盖真实文件事件、共享/释放、符号链接边界、重绑定、导航、取消和活动状态。
- 工作区绑定测试：6/6 通过，包括恢复 38 条绑定仍不开启 chokidar。
- PDF assets / document / runtime 三个相关测试文件：21/21 通过，包括全部 189 个资源与 PDF.js 原始字节比较。
- 桌面完整 typecheck、production build、正式包内容验证和签名验证通过。
- 最终正式包独立临时 profile 的功能 QA：Quick-Ask 已预热；本地旧 `files.watch` 与 `files.observe` 均能观察外部写入、删除、重建，并在取消后停止通知。
- 同一正式包内真实 PDF Worker 验证标准 Symbol 字体字节与字形像素、日文 CMap 字节与页面渲染、JPEG 2000 WASM 解码及红蓝像素、关闭预览后的 Worker 释放；截图已人工检查。

### 明确的验证限制

- PDF 全目录单测中另有两个套件因已发布 DSH 包缺少 `anser` / `zustand` 的可解析开发依赖而无法收集。本次未篡改依赖来绕过，不能声称整个 PDF 测试目录全部通过。
- 原开发插件冒烟入口在正式包中被禁用，正式包功能 QA 因此改用应用实际接口和页面。
- 更广的 PDF 自动刷新脚本在“进程外写文件后自动出现更新提示”处未通过。运行时确认该页面实际使用官方 DSH `workspaceFiles` 资源提供器；已安装 `dsh-api-workspace-files` 文档和 `WorkspaceChangeFeed` 实现明确只转发经 DSH 文件系统产生的 `fs/observed`，不监听操作系统。因此该脚本的外部写入假设不适用于当前提供器，`.cache` 和普通目录均如此。该提供器及其注册路径未改；不把这个脚本记作通过，也未改动依赖。这与上面已通过的 Amiba 本地监听桥验证不同。
- 未测试 Windows/Linux 的正式包，未测长时间工作负载或真实模型首 token 延迟。

## 仍值得做的优化

1. DSH 的模块解析/编译、注册表源码或 source map 缓存、会话 JSONL 恢复仍是 CPU/内存大头。应先建立上游最小复现及测量，再通过正式依赖升级解决，不在 `node_modules` 或第三方产物中直接删改。
2. 继续评估 Amiba 自有插件打包边界，避免同一大资源/模块重复内嵌；每次都应复测实际 DSH 峰值，不能以 JS 文件缩小比例替代内存结论。
3. 自有会话状态轮询可进一步评估事件驱动或增量更新，但需保留后台任务、审批和断线恢复语义。本次没有改轮询协议。
4. 如需预览自动感知外部编辑，单独设计官方资源提供器与本地按需观察之间的适配，避免再引入整目录常驻递归监听。
