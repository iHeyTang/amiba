# DSH 扩展适配阶段归档

归档日期：2026-09-14。按用户要求在当前成果处收尾，停止继续扩展实现。**这是阶段归档，不代表全部兼容完成。** 下表作为当前剩余事项汇总；旧文档中的“仍待完成”可能已被后续进展覆盖，应结合本归档阅读。

代码分支：`feat/dsh-extension-compat-next`；实现检查点：`8b42a19c`；原始 main 基线：`99e8f93`。运行版本为 DSH `0.1.1-rc.2`，官方对照源码固定为 `c291e7961a515f6d7af9304e7fd1d257929aef26`，不代表未来官方版本。当前保留在独立工作树，未合并、推送或部署。

## 已完成到哪里

| 部分 | 当前成果与边界 |
| --- | --- |
| 扩展界面和工具 | 设置、插件配置、附加输入、会话操作、命令结果、工具与审批详情、导航和右侧工作区已有接入；不同范围的桌面验证已通过，不代表所有第三方插件均通过 |
| 预览和资源 | 文本、Markdown、代码、图片、HTML、PDF 预览，以及会话授权读取、取消、监听和资源释放已有实现 |
| 输入和图片 | 图片持久化基础链路、后台文本/命令和部分队列、引用剪贴板、会话内编辑历史已有实现；完整入口及跨窗口覆盖仍见下表 |
| 普通文件命令和上传 | 文件存储、会话上传凭据、命令解析、原输入器文件命令、Host 流式路由、桌面后台分块上传和取消已接通，桌面成功/失败重试场景已验证 |
| 模型文件底层 | 文件块转模型可读路径文本、session.prompt 文件凭据接收、请求计量及压缩计量衔接已接通；受控接收后可从 session.history 读回。尚未完成原输入器端到端模型发送，也未验证外部模型整轮 |

## 还差哪些部分

“未完成”表示仍需适配；“待验证”表示已有部分实现但证据不足。以下各项不因本次归档而自动判定为不支持。

| 剩余部分 | 具体还差什么 | 状态与判断 |
| --- | --- | --- |
| 原输入器向模型发送普通文件 | 将现有文件选择和消息发送接入新的文件凭据/文件块链路，保留原附件能力；完成真实模型整轮验证 | 未完成；可继续适配，底层 API 已具备 |
| 后台文件与队列 | 离屏普通文件注册、文件入队/出队；原生与 Host 双队列顺序、前台目标交接、后台编辑历史合并和 Host inbox 协调 | 未完成；需成组处理，不能直接替换原队列 |
| 提交确认与文件生命周期 | 重复请求确认和幂等性、冷会话上传解析接线、草稿/队列/附件跨窗口所有权与重启恢复 | 未完成；目前失败回滚、重试成功不等于以上场景已覆盖 |
| 文件历史与展示 | 文件历史 UI、预览及完整重启恢复 | 未完成或待验证；已有历史 API 读回证据，不能替代这些验收 |
| 新版输入与引用语义 | 新旧输入坐标、词边界、点击与失效语义，多引用和附件混合历史 | 未完成；rc.2 词表高亮及部分引用行为已有验证 |
| 图片完整场景 | 本人发送图片回显、preview、Quick Ask、第三方整棵工具树、外部打开工具，以及历史回收和跨窗口所有权 | 待补齐或验证；基础图片链路已完成，不能据此推断所有入口完成 |
| 全部扩展及新版依赖 | 原 64 个入口逐条完成组件和服务依赖核对，尤其 20 个替换类入口的附加能力或可选视图；插件失败、禁用和卸载回退 | 未完成；有条件适配，必须保留原默认能力和样式。44 个有接入基础、20 个有条件适配是分类，不是完成率 |
| 独立 Web 和远程连接 | 真实 Web 部署下 Worker/流式上传及浏览器网络条件；断线重连、远程资源、目录、下载和连接语义 | 待适配或验证；桌面通过不能代替 Web 通过，新版浏览器认证也未迁入 |
| 最终整体验收 | 完整 64 项和相关服务的最终状态表，以及默认功能、样式和插件组合回归 | 未执行完整收尾验收；本次按要求停止，不继续增加测试轮次 |

## 有确切原因的限制

| 能力 | 已确认原因 | 本阶段处理 |
| --- | --- | --- |
| 一次性子会话继续发送或停止 | rc.2 公共接口拒绝 | 不能通过当前公共接口完整对齐，需上游变更或另行设计 |
| 可续聊子会话向模型发送图片 | rc.2 公共接口不支持；图片命令是另一条能力 | 保留已有功能，不宣称模型图片已支持 |
| 冷嵌套子会话恢复 | 要求直接父 Agent 驻留，缺少公共的无消息冷恢复操作 | 当前条件下无法完整对齐 |
| Host 队列编辑图片和子会话目标 | rc.2 仅支持文本并拒绝子会话目标 | 保留原生图片/子会话队列，不能直接全量替换 |
| 官方子会话普通文件上传 | 固定对照版本明确拒绝，错误为 SUBAGENT_FILE_UNSUPPORTED | 归为上游限制，保留 Amiba 原生子会话附件能力 |
| 插件依赖私有 DOM、Electron API 或丢失的源位置 | 私有结构无通用契约；纯浏览器缺少宿主 API；丢失的 Markdown 映射无法凭空恢复 | 只能按具体插件和宿主评估专用适配，不承诺通用完整支持 |

重启后恢复整个撤销/重做栈不计入缺口：已核对的官方版本也不持久化完整编辑历史。草稿、队列和附件的恢复仍是上表中的待办。

## 验证与后续接手

最后一次实现检查点已通过 37 项附件测试、类型检查、架构检查、运行时构建与校验，以及实际桌面兼容、输入状态和图片命令回归。文件模型接收使用受控 Agent 接收器，没有调用外部模型。以上证据不覆盖全部插件、平台及重启场景。本次归档只改文档，不重复运行完整构建。

如以后恢复，优先完成“原输入器模型文件发送 → 队列与生命周期 → 独立 Web 和完整扩展验收”，按功能组集中实现和验证。

详细依据：[逐项评估表](dsh-extension-adaptation-assessment.zh-CN.md)、[历史收尾清单与进展](dsh-extension-closeout.zh-CN.md)、[实施记录](dsh-extension-compatibility-plan.md)。历史记录保留用于追溯，不再作为继续自动推进的指令。
