# DSH 扩展兼容收尾清单

目标保持不变：以 main 为基线接入所有可兼容能力，保留 Amiba 已有能力和样式。范围仍为原审计的 64 个入口、相关服务语义，运行基线 DSH 0.1.1-rc.2，新版对照固定 c291e796。44 个入口有接入基础、20 个替换类入口有条件适配，不是完成率。

## 已有成果

设置和插件配置、附加输入入口、工具/审批详情、导航和祖先关系、右侧工作区、文本/代码/图片/HTML/PDF 预览、会话授权文件服务、消息/工具/轨迹图片基础链路、后台输入和部分队列、引用剪贴板与会话历史均已有实现及不同范围的桌面验证。每项实际覆盖范围仍以 [64 项详细表](dsh-extension-adaptation-assessment.zh-CN.md) 为准；不能据此推断全部插件、平台、编码变体均完成。

## 剩余工作按功能组处理

| 功能组 | 已知尚未完成或尚未证实的范围 | 本组收尾证据 |
| --- | --- | --- |
| 输入和队列 | 普通/编辑队列发送的接收确认与提交前历史消费已接通；新稿多步历史和会话往返通过编辑器测试，编辑队列真实快捷键通过桌面验证；原生/Host 双队列顺序、前台目标交接；后台编辑历史合并；草稿/队列跨窗口、重启和 Host inbox 协调 | 真实接收/拒绝/未确认结果，失败保留、新稿保护、顺序和一次派发；对应跨会话/重启场景。不能以 client.submit 返回代替接收确认 |
| 引用、附件和图片 | rc.2 公开词表高亮已通过桌面验证；新旧输入坐标、词边界及失效语义；多引用/附件混合历史；附件跨窗口所有权、历史回收；本人发送图片回显、preview、Quick Ask、第三方整树工具与外部打开工具 | 核对实际公共契约后补实现；真实文件、模型/命令输入、引用身份和生命周期回归。未接线的内部类型不算官方已支持能力 |
| 完整插件与版本依赖 | 全部 64 个入口的完整组件依赖、私有服务使用；20 个替换类入口的附加能力或可选视图；新版服务尚未逐个闭环 | 每行记录可运行的契约和组件证据。保留原默认界面；插件禁用、失败、卸载后原操作可用 |
| 连接恢复与独立 Web | 流式断线/重连恢复；独立 Web/远程资源、目录、下载、连接行为及桌面专属能力替代 | 在相应运行环境验证。桌面通过不代替 Web 通过；纯浏览器缺少系统 API 的能力明确说明宿主条件 |
| 最终整体核对 | 清理旧清单的过时状态，核对默认功能/样式和所有未完成项 | 每个入口和服务要求标注“已验证 / 未完成或未验证 / 有确切限制”，形成一个汇总交付。全部必要项有证据前不宣布完成 |

## 已确认的上游限制

- rc.2 一次性子会话不允许通过公共接口继续发送或停止。
- rc.2 可续聊子会话不支持向模型发送图片；文本与图片命令不是同一能力。
- 冷嵌套子会话需直接父 Agent 驻留；缺少恢复冷子父会话的公共无消息操作。
- rc.2 Host 队列编辑仅支持文本，拒绝子会话目标；不能直接替换原生图片/子会话队列。
- 私有 DOM 依赖、纯浏览器的 Electron API、已丢失的 Markdown 源位置映射不能保证通用兼容；可行专用适配仍按具体组件核对。

## 执行约束

按上述功能组集中实现和回归，不将每个边界用例拆成独立阶段。没有生产代码变更或新失败依据时，不重复完整构建和桌面回归。必要失败应修复并记录；新发现的问题归入原功能组，不能据此不断扩张目标。保留原始 64 项范围，不能把尚未验证项改称上游不支持来提前收尾。

## 本轮进展

2026-09-14 进展：rc.2 公开词表装饰已接入原生编辑器。真实桌面验证了插件注册、异步词表变化和卸载，正文、引用列表、草稿版本和输入框尺寸保持不变。新版词边界、点击和输入协议仍待核对；不支持 CSS Custom Highlight 的浏览器暂保留普通文本显示，独立 Web 尚未验证。

## 不计入兼容缺口的额外增强

重启后恢复整个撤销/重做栈：已核对 rc.2 与 c291e796，官方新建空历史，只镜像和恢复草稿。该能力不是已审计官方契约中的支持项，不再把它作为无限延伸的收尾条件。草稿、队列和附件的持久化要求不受此纠正影响。


2026-09-14 后续进展：新旧图片命令的能力声明、附件数量和调用载荷已分别适配；95 项测试及同次真实桌面新旧命令回归通过。普通文件 receipt、完整新版附件操作和输入坐标仍未完成，详见详细评估表的新旧输入契约对照。


文件 receipt 审计：普通根会话需要附件存储、命令 receipt 解析、上传服务和生命周期整体迁移，当前运行时缺少相关公开契约；不是单个输入字段缺失。固定 c291e796 的子会话文件上传明确被上游拒绝（SUBAGENT_FILE_UNSUPPORTED）。两者已在详细表分开列出，原生子会话附件功能保留。


文件存储进展：已为当前 Host 附件服务补齐官方普通文件持久化、流式读写和完整性校验，真实 Host 文件与原图片能力同轮验证通过；卸载和许可证打包已处理。仍待 receipt 解析、上传授权与浏览器文件生命周期，不再把文件存储本身列为完全缺失。


2026-09-14 文件授权进展：新增 Host fileUploads 服务，复用普通文件存储，按精确 Agent/Session 身份签发上传凭据；提供接收绑定、失败回滚、成功后的按请求回收，以及会话释放清理。5 项授权测试和原有 11 项附件测试通过。rc.2 不导出新版 RemoteError，当前使用显式 Host FileUploadError，远程边界仍需映射；浏览器上传、命令 receipt 解析和队列实际调用尚未接通，不能据此将普通文件提交列为完成。

真实 Host 验证补充：安装后的 fileUploads 与实际 Agent 已完成上传、文件解析、接收失败回滚及成功绑定后回收验证；同次桌面新旧图片命令、混合粘贴和原输入历史回归通过。运行时 prepare/verify、类型检查与架构检查通过。此证据不覆盖远程上传端点、文件命令或模型文件提交。


命令文件接收进展：通过固定 rc.2 补丁增加 registerFileReceiptResolver，并与 Host fileUploads 的精确会话授权接通；命令可接收文件/图片混合载荷，先验证全部文件权限再写入图片，保持顺序、取消和生命周期。旧 images 声明继续使用；显式 attachments 声明优先。Host 网关兼容 images 与 submittedAttachments 两种字段，禁止同时提供。命令与附件共 28 项测试、类型和架构检查、运行时校验通过；真实桌面旧客户端和新版 HTTP 字段均通过文件命令执行，未知凭据拒绝，新旧图片命令与混合粘贴回归通过。浏览器上传与输入消费仍未完成。


浏览器上传进展：已增加带 Agent lookup 的 fileUploads/upload 严格远程描述、rc.2 结构化错误映射，以及客户端 fileUpload 服务。支持 Uint8Array 精确视图和 Blob 的编码上传、取消，保留原附件上传。客户端服务保留自身远程授权，调用插件无需额外依赖 remote.fileUploads；已有 fileUpload 实现不替换。23 项测试、类型和架构检查、运行时校验通过；真实桌面 Blob 上传获得新凭据并通过官方命令执行、取消及原输入能力回归通过。available 当前为 false，明确表示流式后台承载尚未接入；ReadableStream 不会被偷偷读入整块内存。原生输入器的普通文件调用与消费、流式上传、队列仍待完成。


原输入器文件命令进展：已接通原文件选择器到官方文件命令的准备和消费。真实桌面验证了 Host 拒绝后原草稿/附件保留、重试成功后对应正文与附件清理，Host 两次收到的字节一致；原图片、混合粘贴和历史回归通过。52 项相关测试、类型/架构及构建校验通过。离屏普通文件注册、队列/跨窗口生命周期、流式上传和模型文件仍待完成。


2026-09-14 流式 Host 通道进展：为 rc.2 Connection 增加按调用插件生命周期注册的精确 Fetch 路由和 streaming 请求模式；普通 RPC 继续缓冲并执行原大小上限，上传继续经过现有 Host/Origin 来源检查。注意 rc.2 的来源检查不是新版浏览器 token/cookie 认证，本次没有声称迁入新版认证。新增 /api/session/uploadFileBinary，将请求分块直接交给精确会话授权的 fileUploads.uploadStream，连接断开传递取消，失败返回结构化错误。实际 HTTP 测试证明请求 EOF 前已接收分块，并验证跨站拒绝、普通 RPC 上限及卸载回退；真实桌面安装后的路由签发凭据并成功执行官方文件命令。附件原有 23 项测试与新增 2 项 HTTP 测试通过，类型、架构、运行时构建/校验和桌面回归通过。浏览器后台 Worker/ReadableStream 调用仍待接入，客户端 available 仍为 false；不能据此宣布端到端后台流式上传完成。


2026-09-14 后台客户端上传进展：fileUpload 服务新增 Blob/ReadableStream 后台承载，保留 Uint8Array 编码 Remote 回退。网页端适配官方 Worker 的 Blob XHR 与流式 fetch；桌面通过预启动 __DSH_FILE_UPLOAD__ hook 使用窗口隔离的 IPC，每块最多 64 KiB，等待写入确认再读取，Host 地址和路径固定校验。窗口关闭/导航、调用取消及提供服务的插件卸载会取消对应上传，回收 IPC 状态；进度和结构化结果已接通。实际桌面 Blob 与分块 ReadableStream 上传均获得有效凭据并执行官方文件命令；多块传输中取消触发 AbortError 并取消源读取，原文件/图片命令、粘贴和历史回归通过。28 项附件测试、9 项运输/分块测试、插件与桌面类型检查、架构检查、完整构建及运行时/桌面包验证通过。桌面客户端 available 为 true；网页 Worker 有逻辑测试，独立 Web 实际部署、浏览器网络协议条件仍待验收，不能把桌面结果外推到所有 Web 环境。普通文件模型输入、离屏注册、队列及跨窗口文件语义仍未完成。


2026-09-14 模型文件表示进展：核对固定 c291e796 后确认，官方普通文件从不以原生文件块发送给模型提供方，而是在最终 adapter 调度前转换为只读文件路径文本。已给 rc.2 LLM 补齐 contentHasFile、fileHandleText、projectFilesToText 和 fileRequestText，递归覆盖工具结果，保留不可变原消息及不相关消息身份；先转换文件，再按原策略处理不支持图片的模型。补齐 FileAttachmentRef/FileBlock 类型，以及 FS 的保守默认路径映射和 Local FS 的绝对路径映射；未知执行环境或坏引用明确返回无法读取路径的说明，未扩大文件写权限。4 项新增真实 LLM/存储测试及全部 32 项附件测试、类型/架构、运行时构建/校验通过；真实桌面 Host 的 fileRequestText 返回实际存储文件路径，同轮流式上传、命令及原输入能力回归通过。新增开发测试依赖不改变受管运行时的 DSH 版本。模型提交入口、token 统计、队列和完整文件历史仍待接通，不能将请求转换层完成等同端到端文件模型提交完成。


2026-09-14 文件模型提交 API 进展：rc.2 session.prompt 的 Host 与实际浏览器发布包均接受 {type:'file',receiptId}；公共 PromptContentPart 类型同步扩展。Host 在任何图片持久化前验证整批文件的精确会话凭据，保持文字/文件/图片及重复文件的原顺序，生成结构化 UserMessage；接收时按 request.rpcId 绑定文件，失败回滚、成功提交，接收前核对 Agent 仍驻留。未知凭据返回 attachment-error/FILE_NOT_STAGED。33 项附件测试、类型/架构和运行时构建/校验通过；真实桌面安装后的 session.prompt 首次受控拒绝、同凭据重试成功及原输入回归通过。该桌面用例在 Agent 接收边界使用受控接收器，不调用外部模型；不覆盖真实模型整轮、持久历史和队列出队。原输入器模型文件发送、离屏注册、队列、token 统计及历史展示仍待闭环。


2026-09-14 文件计量和历史进展：TokenMeter.measure 使用与 LLM 相同的文件路径文本估算模型请求；已有路径映射变化时重放该会话计量，保留正常文本行为、嵌套文件、历史替换及 provider 用量基准。公共 estimateMessage 仍维持原结构估算，用于工具结果裁剪；当请求值与结构值不同时，节点额外携带 heuristicTokens，官方 basic compaction 写入结构 shadowedTokenCount，避免持久化投影扣减错位。4 项新增测试覆盖上述行为和压缩准备/投影联动；全部 37 项附件测试、类型、架构和运行时构建/校验通过。补丁合并为从原始包到最终内容的一份差异，独立应用和实际打包均通过。真实桌面中，受控接收器将成功的文件消息实际写入 Session，再经 session.history 读回，文件标识、顺序和来源保持；真实 Host 的文件请求计量与同份路径文本一致，同轮原输入回归通过。没有外部模型调用；历史 API 读回不代表重启恢复或 UI 文件展示完成。原输入器模型文件发送、重复请求确认、队列/重启/跨窗口生命周期与文件展示仍待闭环。
