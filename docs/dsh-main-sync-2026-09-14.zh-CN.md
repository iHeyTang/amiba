> 本文保留历史审计／设计背景；官方插槽的当前状态及后续接入、同步、退役决策统一维护于 [DSH 官方插槽兼容维护台账](../DSH-SLOT-COMPATIBILITY.md)，不再在本文更新插槽当前状态。

# 兼容分支同步 main 记录

2026-09-14 将拉取到的最新 `origin/main`（`3d57d8a9c5b8ef32a4c1cd815898cde45c0c9418`）合入 `feat/dsh-extension-compat-next`。合并前兼容分支为 `e22e22d5`，工作区干净。本次是 main 进入兼容分支，不是将兼容分支发布或合入 main。

## 冲突和处理

Git 报告的 10 个冲突文件均已解决，并核对了自动合并的桌面 preload 和标题区域。

| 交叉改动 | 合并结果 |
| --- | --- |
| 会话恢复与附件发送 | 保留 main 的既有 IM/插件会话目录、预设恢复；新会话才使用桌面创建默认值。保留兼容分支独立的子会话续聊路径、附件授权、文件/图片提交和接收确认 |
| 运行时依赖安装 | 采用 main 的 Host/插件隔离安装方式；预览所需 shiki、PDF 等依赖归属插件。补充兼容设置卡所需的两个 Host 接口，重新生成受影响的分发清单与锁文件 |
| 补丁与缓存 | 使用 main 的跨平台 Git 补丁工具，保留 Windows HMR 修复及所有兼容补丁；嵌入式 ui-primitives 对应实际 Web frontend 补丁。缓存继续核对补丁内容摘要，补丁变更不能复用旧树 |
| 界面与新功能 | 保留 main 的标题宽度修复、同步相关界面、新版管家/引导/连接器改动；兼容分支的标题角落、祖先导航及原输入器能力继续保留 |
| 回归用例 | 更新会话列表读取的测试接口，以及分散到两个文件的管家工具注册清单。通用设置槽位单测隔离依赖浏览器加载器的内置卡片；内置卡片仍由实际桌面回归验证 |

## 验证结果

- 聊天引擎与消息相关测试：159 项通过，包含 IM 会话恢复、子会话、文件输入和接收确认。
- 插件外壳：首次运行 877 项通过；两处旧测试环境/清单问题修正后，涉及的两套用例共 51 项通过（其中包含重跑项）。
- UI/消息/命令附件：76 项；模型配置和引导：51 项；连接器：203 项；附件服务：38 项，均通过。
- 运行时安装/补丁/隔离/复用：12 项；发布配置检查：28 项，均通过。
- 类型检查、架构检查、完整桌面构建、桌面包校验和运行时 verify 均通过。
- 实际桌面回归通过：普通文件命令与模型发送、拒绝后的队列保留和重试、本地测试模型回复、刷新后的文件记录、图片命令、混合粘贴、输入状态、子会话续聊和停止、标题角落扩展、祖先导航、插件配置和卸载回退。

本轮实际运行环境为 macOS 桌面、DSH `0.1.1-rc.2`、受管 Node `22.22.0`。模型整轮使用本地测试适配器，没有调用外部模型。未重新验收 Windows、独立 Web、全部第三方插件或此前归档的跨窗口/重启完整生命周期；不能把本次同步通过扩大为这些范围全部兼容。

剩余功能仍见 [兼容归档](dsh-extension-compatibility-archive.zh-CN.md)。本次没有扩展原功能待办，也没有推送、发布或修改 main 工作树。
