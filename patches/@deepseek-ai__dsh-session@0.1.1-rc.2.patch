diff --git a/lib/index.js b/lib/index.js
index 62fd0fe17593f5b2d52b8a792add164e1f16a4d1..63224732653bd183c1e325ab9a4c5a9b00ade1b6 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -1443,7 +1443,9 @@ var Session = class Session {
 	*/
 	append(type, data, ...opts) {
 		const surfaceOpts = opts[0];
+		if (surfaceOpts?.ignorable !== void 0 && surfaceOpts.ignorable !== true) throw new Error("ignorable must be true when provided");
 		const surfaceMetadata = {
+			...surfaceOpts?.ignorable === true ? { ignorable: true } : {},
 			...surfaceOpts?.sourceEventSeqs === void 0 ? {} : { sourceEventSeqs: surfaceOpts.sourceEventSeqs },
 			...surfaceOpts?.surfaceOp === void 0 ? {} : { surfaceOp: surfaceOpts.surfaceOp }
 		};
diff --git a/lib/types/index.d.ts b/lib/types/index.d.ts
index 1e2f5ba53021445c9b50d993785f3685cc5e885e..187715a29e4e9bfded8727f686a9c9466e5b6500 100644
--- a/lib/types/index.d.ts
+++ b/lib/types/index.d.ts
@@ -209,7 +209,7 @@ export declare class Session {
      *   append reentered while this acceptance/publication boundary is open also
      *   rejects before the log changes.
      */
-    append<T extends SessionEventType>(type: T, data: SessionEventMap[T], ...opts: T extends SurfaceEventType ? [opts: SurfaceIntent] : []): SessionEvent<T>;
+    append<T extends SessionEventType>(type: T, data: SessionEventMap[T], ...opts: T extends SurfaceEventType ? [opts: SurfaceIntent & { ignorable?: true }] : [opts?: { ignorable?: true }]): SessionEvent<T>;
     /** Cached fold of the request-header events — see {@link requestHeader}. */
     private headerFold;
     /** Log position (events consumed) the header fold has reached. */
