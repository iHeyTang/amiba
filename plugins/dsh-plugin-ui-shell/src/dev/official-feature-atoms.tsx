// The standalone fixture does not boot DSH's browser module loader. Only the
// PlanChip close glyph is needed; production uses the official external icon.
export { X as IconCloseFill14 } from "lucide-react";
