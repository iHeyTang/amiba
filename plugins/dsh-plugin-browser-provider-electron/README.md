# @amiba/dsh-plugin-browser-provider-electron

Electron provider for `@amiba/dsh-plugin-browser-core`.

It registers one high-priority provider backed by the authenticated loopback native-operation gateway. It owns no tool name, JSON schema, provenance, attachment persistence or browser-domain state; those remain in the browser core plugin.
