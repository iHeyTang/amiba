import { ModelProviderConfigTab } from "./ModelProviderConfigTab";

/** Harness-independent provider, model, credential, and connection settings. */
export function AgentModelConfigTab() {
  return <ModelProviderConfigTab />;
}
