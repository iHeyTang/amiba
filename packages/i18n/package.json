{
  "name": "@amiba/i18n",
  "version": "0.1.0",
  "private": true,
  "description": "The i18n MECHANISM \u2014 useT()/usePluginT plus the two realm registries (which language is active, and where the templates come from). Ships no dictionary: the catalogs live with their owners (@amiba/ui/locales, the ui-shell's own locales, apps/desktop's window copy) and are registered as one official namespace by the shell.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./plugin": "./src/plugin.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@amiba/app-runtime": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "18.3.12",
    "typescript": "5.6.3",
    "vitest": "2.1.4"
  }
}
